const Discord = require("discord.js");
module.exports = {
  name: "messageReactionAdd",
  run: async (bot,messageReaction,user) => {
    // if(messageReaction.message.guild.id === bot.supportserver){
      // console.log(messageReaction.emoji.name)
      // console.log(messageReaction.emoji.id)
      // console.log(messageReaction.emoji.identifier)
      // console.log(messageReaction.message.reactions.cache.keyArray())
    // }
  }
}